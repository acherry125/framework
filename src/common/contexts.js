import React from 'react';

export const FormSubmitContext = React.createContext(); // pass to objects which trigger submission events

const contexts = {
  FormSubmitContext
}

export default contexts;